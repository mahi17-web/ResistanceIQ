/**
 * ResistanceIQ — Production Notice
 * Mock data has been completely removed in Step 12.
 * All application layers connect to live FastAPI backend endpoints (/api/v1).
 */
export const MOCK_DATA_DEPRECATED = true;
